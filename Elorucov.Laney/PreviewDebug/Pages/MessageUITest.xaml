<Page
    x:Class="Elorucov.Laney.PreviewDebug.Pages.MessageUITest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.PreviewDebug.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    SizeChanged="Page_SizeChanged"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid x:Name="code" Margin="6">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBox x:Name="json" TextWrapping="Wrap" FontFamily="Lucida Console" FontSize="12" AcceptsReturn="True" PlaceholderText="Enter message object JSON"/>
            <Button Grid.Row="1" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Margin="0,6,0,0" Content="Render" Click="Button_Click"/>
        </Grid>
        <ScrollViewer x:Name="preview" Margin="6">
            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Text="Message UI displays here..."/>
        </ScrollViewer>
    </Grid>
</Page>
