<Page
    x:Class="Elorucov.Laney.PreviewDebug.Pages.FileUploader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.PreviewDebug.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel MaxWidth="576">
            <Button HorizontalAlignment="Stretch" Margin="12,12,12,0" Content="Get Photo Upload Server" Click="PhotoServer"/>
            <Button HorizontalAlignment="Stretch" Margin="12,12,12,0" Content="Get Video Upload Server" Click="VideoServer"/>
            <Button HorizontalAlignment="Stretch" Margin="12,12,12,0" Content="Get Doc. Upload Server" Click="DocServer"/>
            <Button HorizontalAlignment="Stretch" Margin="12,12,12,0" Content="Get Doc. Upload Server (audiomessage)" Click="DocServerAudio"/>
            <Button HorizontalAlignment="Stretch" Margin="12,12,12,0" Content="Get Audio message Upload Server (new)" Click="AudioMsgServer"/>
            <ToggleSwitch x:Name="um" Margin="12,12,12,0" OnContent="Using HttpClient" OffContent="Using BackgroundUploader"/>
            <StackPanel Orientation="Horizontal">
                <Button Margin="12,12,12,0" Content="Upload" Click="Upload"/>
                <Button Margin="0,12,12,0" Content="Save" Click="Save"/>
                <Button Margin="0,12,12,0" Content="Cancel" Click="Cancel"/>
            </StackPanel>
        </StackPanel>
        <TextBox x:Name="UploadUrl" Grid.Row="1" Margin="12,12,12,0" Header="Upload URL"/>
        <TextBox x:Name="res" TextWrapping="Wrap" IsReadOnly="True" Grid.Row="2" Margin="12" FontFamily="Lucida Console" Text="Firstly, press the &quot;Get ... Upload Server&quot;. To upload graffiti, get link via official client's token, then click &quot;Get Doc. Upload Server&quot; and replace link in &quot;Upload URL&quot; field." FontSize="12"/>
    </Grid>
</Page>
