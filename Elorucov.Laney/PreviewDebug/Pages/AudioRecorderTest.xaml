<Page
    x:Class="Elorucov.Laney.PreviewDebug.Pages.AudioRecorderTest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.PreviewDebug.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrl="using:Elorucov.Laney.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel Margin="12">
            <Button Margin="0,0,0,12" Content="Show UI" Click="ShowUI"/>
            <Grid Margin="0,0,0,12" Width="296" Height="96" BorderBrush="Blue" Background="#1f7f7f7f" BorderThickness="2" HorizontalAlignment="Left">
                <Button VerticalAlignment="Top" Content="Clickable"/>
                <ctrl:AudioRecorderControl x:Name="audioRec"/>
            </Grid>
            <Button Margin="0,0,0,12" Content="Start recording" Click="OnStart"/>
            <Button Margin="0,0,0,12" Content="Stop" Click="OnStop"/>
            <TextBlock x:Name="res" TextWrapping="Wrap"/>
        </StackPanel>
    </ScrollViewer>
</Page>
