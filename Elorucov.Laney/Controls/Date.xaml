<UserControl
    x:Class="Elorucov.Laney.Controls.Date"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:ms="using:Microsoft.UI.Xaml.Media"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Rectangle x:Name="ShadowContainer" RadiusX="12" RadiusY="12" Fill="{ThemeResource VKBackgroundContentBrush}"/>
        <Border x:Name="Root" CornerRadius="12" Background="{ThemeResource VKBackgroundContentBrush}">
            <HyperlinkButton Padding="0" BorderThickness="0" Click="OnClick">
                <TextBlock x:Name="dt" Foreground="{ThemeResource VKTextSubheadBrush}" FontSize="12" Margin="8,3,8,5"/>
            </HyperlinkButton>
        </Border>
    </Grid>
</UserControl>
