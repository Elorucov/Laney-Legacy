<UserControl
    x:Class="Elorucov.Laney.Controls.BotCarouselElement"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="270"
    d:DesignWidth="270">

    <HyperlinkButton Padding="0" Click="ElementClicked" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch">
        <Grid Width="270" CornerRadius="14" Background="{ThemeResource VKImBubbleIncomingBrush}">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel VerticalAlignment="Top">
                <Image x:Name="CardImageContainer" Width="270" Height="166" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="UniformToFill">
                    <Image.Source>
                        <BitmapImage x:Name="CardImage" DecodePixelType="Logical" DecodePixelWidth="270" DecodePixelHeight="166"/>
                    </Image.Source>
                </Image>
                <TextBlock x:Name="CardTitle" Visibility="Collapsed" Margin="12,8,12,0" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap" MaxLines="2"/>
                <TextBlock x:Name="CardDescription" Visibility="Collapsed" Margin="12,4,12,12" FontSize="14" Foreground="{ThemeResource VKTextSubheadBrush}" TextWrapping="Wrap" MaxLines="2"/>
            </StackPanel>
            <StackPanel x:Name="ButtonsContainer" Grid.Row="1" VerticalAlignment="Bottom" Margin="8,4,8,8"/>
        </Grid>
    </HyperlinkButton>
</UserControl>
