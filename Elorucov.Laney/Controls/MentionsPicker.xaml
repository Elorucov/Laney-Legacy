<UserControl
    x:Class="Elorucov.Laney.Controls.MentionsPicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mdl="using:Elorucov.Laney.Models"
    xmlns:elor="using:Elorucov.Toolkit.UWP.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="Transparent" Tapped="Hide">
        <Border VerticalAlignment="Bottom" Background="{ThemeResource AcrylicInAppFillColorDefaultBrush}" BorderBrush="{ThemeResource SurfaceStrokeColorFlyoutBrush}" BorderThickness="1" CornerRadius="{ThemeResource OverlayCornerRadius}" Margin="12">
            <ListView x:Name="mentionslist" MaxHeight="236" ItemContainerTransitions="{x:Null}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="Mentionslist_ItemClick">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="mdl:MentionItem">
                        <Grid Margin="0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <elor:Avatar Width="40" Height="40" ImageUri="{x:Bind Avatar}" DisplayName="{x:Bind Name}"/>
                            <StackPanel Grid.Column="1" Margin="10,0,0,2" VerticalAlignment="Center">
                                <TextBlock x:Name="TitleString" FontSize="15" MaxLines="1" Text="{x:Bind Name}"/>
                                <TextBlock x:Name="DescString" Margin="0,0,0,0" FontSize="14" FontWeight="SemiLight" MaxLines="1" Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}" Text="{x:Bind Domain}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</UserControl>
