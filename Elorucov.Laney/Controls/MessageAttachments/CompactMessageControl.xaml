<UserControl
    x:Class="Elorucov.Laney.Controls.MessageAttachments.CompactMessageControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Controls.MessageAttachments"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <Style x:Key="AltAccentStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{ThemeResource AlternativeAccentBrush}"/>
        </Style>
        <Style x:Key="ConvHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{ThemeResource ConvViewHeaderForegroundBrush}"/>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="Root" Height="36">
        <Rectangle x:Name="LeftStrip" HorizontalAlignment="Left" Fill="{ThemeResource AlternativeAccentBrush}"/>
        <Grid x:Name="ContentGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border x:Name="ImageContainer" Visibility="Collapsed" Margin="0,0,10,0" Width="36" Height="36" CornerRadius="3">
                <Image HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="UniformToFill">
                    <Image.Source>
                        <BitmapImage x:Name="Image" DecodePixelType="Logical"/>
                    </Image.Source>
                </Image>
            </Border>
            <StackPanel VerticalAlignment="Center" Grid.Column="1" Margin="0,-2,0,0">
                <TextBlock x:Name="Sender" FontSize="14" FontWeight="SemiBold" MaxLines="1" TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="MessageText" FontSize="14" Foreground="{ThemeResource VKTextSubheadBrush}" MaxLines="1" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
