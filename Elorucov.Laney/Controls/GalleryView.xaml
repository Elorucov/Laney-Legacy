<UserControl
    x:Class="Elorucov.Laney.Controls.GalleryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Controls"
    xmlns:vkui="using:VK.VKUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="Root" SizeChanged="RootSizeChanged">
        <vkui:Spinner x:Name="loader" Width="44" Height="44" Foreground="{ThemeResource ApplicationForegroundThemeBrush}"/>
        <Viewbox x:Name="vb">
            <ScrollViewer x:Name="sv" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden" ZoomMode="Enabled" MinZoomFactor="1" MaxZoomFactor="2" SizeChanged="ChangeZoomFactor" >
                <Image x:Name="imgz" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </ScrollViewer>
        </Viewbox>
        <vkui:VKIcon x:Name="errorIcon" x:DeferLoadStrategy="Lazy"  Id="Icon56ErrorOutline"/>
        <TextBlock x:Name="dbg" Margin="0,32,0,0" FontSize="12" FontFamily="Lucida Console" IsHitTestVisible="False">
            <Run x:Name="da" Foreground="Yellow"/>
            <LineBreak/>
            <Run x:Name="db" Foreground="GreenYellow"/>
            <LineBreak/>
            <Run x:Name="dw" Foreground="LawnGreen"/>
            <LineBreak/>
            <Run x:Name="dc" Foreground="LawnGreen"/>
            <LineBreak/>
            <Run x:Name="dp" Foreground="ForestGreen"/>
            <LineBreak/>
            <Run x:Name="de" Foreground="Red"/>
            <LineBreak/>
            <Run x:Name="df" Foreground="Cyan"/>
        </TextBlock>
    </Grid>
</UserControl>
