<Page
    x:Class="Elorucov.Laney.Pages.FeedView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Pages"
    xmlns:ctrls="using:Elorucov.Laney.Controls"
    xmlns:vkobj="using:Elorucov.VkAPI.Objects"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:converters2="using:Elorucov.Laney.Services.Converters.MessageItem"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" SizeChanged="OnSizeChanged">

    <Page.Resources>
        <converters2:NullToVisibility x:Key="ntv"/>
        <converters2:NullToVisibility2 x:Key="vtn"/>
        
        <DataTemplate x:Key="PostTemplate" x:DataType="vkobj:WallPost">
            <Grid MaxWidth="720" Margin="12,5" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="{StaticResource OverlayCornerRadius}">
                <!--<ctrls:PostUI Margin="12" Post="{x:Bind}"/>-->
                <Border Margin="12" x:Name="PostContainer" DataContextChanged="PostContainer_DataContextChanged"/>
            </Grid>
        </DataTemplate>
    </Page.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid x:Name="HeaderWide" MaxWidth="720" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="12"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <HyperlinkButton x:Name="BackButtonHW" x:Uid="backButton" Grid.Column="0" Height="48" Width="48" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" Content="" Click="GoBack"/>
            <TextBlock x:Name="WallOwnerNameHW" Grid.Column="1" VerticalAlignment="Center" Style="{StaticResource BaseTextBlockStyle}" FontSize="15" LineHeight="22" TextTrimming="CharacterEllipsis" Text="{Binding OwnerName}"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <HyperlinkButton x:Name="RefreshButtonHW" Height="48" Width="48" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" Content="" Command="{Binding RefreshCommand}"/>
            </StackPanel>
            <AutoSuggestBox x:Name="SearchBoxHW" Grid.Column="3" Margin="0,8,12,8" Width="320" Visibility="{Binding SearchAvailable, Converter={StaticResource ntv}}" x:Uid="feed_search" QuerySubmitted="DoSearch" Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <AutoSuggestBox.QueryIcon>
                    <SymbolIcon Symbol="Find"/>
                </AutoSuggestBox.QueryIcon>
            </AutoSuggestBox>
        </Grid>

        <Grid x:Name="HeaderNarrow" MaxWidth="720">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="12"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="BackButtonBindedVisibilityWrapper" Visibility="{Binding SearchMode, Converter={StaticResource vtn}}">
                <HyperlinkButton x:Name="BackButtonHN" x:Uid="backButton" Grid.Column="0" Height="48" Width="48" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" Content="" Click="GoBack"/>
            </Border>
            <TextBlock x:Name="WallOwnerNameHN" Grid.Column="1" Visibility="{Binding SearchMode, Converter={StaticResource vtn}}" VerticalAlignment="Center" Style="{StaticResource BaseTextBlockStyle}" FontSize="15" LineHeight="22" TextTrimming="CharacterEllipsis" Text="{Binding OwnerName}"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal" Visibility="{Binding SearchMode, Converter={StaticResource vtn}}">
                <HyperlinkButton x:Name="RefreshButtonHN" Height="48" Width="48" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" Content="" Command="{Binding RefreshCommand}"/>
                <HyperlinkButton x:Name="SearchButtonHN" x:Uid="searchButton" Height="48" Width="48" Visibility="{Binding SearchAvailable, Converter={StaticResource ntv}}" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" Content="" Click="GoToSearchMode"/>
            </StackPanel>

            <HyperlinkButton x:Name="SearchCloseButtonHN" Grid.Column="0" Visibility="{Binding SearchMode, Converter={StaticResource ntv}}" Height="48" Width="48" FontSize="16" FontFamily="{StaticResource SymbolThemeFontFamily}" Content="" Click="ExitFromSearchMode"/>
            <AutoSuggestBox x:Name="SearchBoxHN" Grid.Column="1" Grid.ColumnSpan="2" Visibility="{Binding SearchMode, Converter={StaticResource ntv}}" Margin="0,8,12,8" PlaceholderText="Search posts" QuerySubmitted="DoSearch" Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <AutoSuggestBox.QueryIcon>
                    <SymbolIcon Symbol="Find"/>
                </AutoSuggestBox.QueryIcon>
            </AutoSuggestBox>
        </Grid>

        <ListView x:Name="PostsLV" Grid.Row="1" VerticalAlignment="Top" ItemsSource="{Binding Posts}" SelectionMode="None" ItemTemplate="{StaticResource PostTemplate}" Loaded="InitializeIncrementalLoading">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="MaxWidth" Value="720"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.Header>
                <Border x:Name="ComposerRoot" MaxWidth="696" MinHeight="137" Margin="12,0,12,5" Visibility="{Binding Composer, Converter={StaticResource ntv}}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="{StaticResource OverlayCornerRadius}">
                    <ctrls:PostComposer x:Name="Composer" DataContext="{Binding Composer}"/>
                </Border>
            </ListView.Header>
            <ListView.Footer>
                <Grid MaxWidth="720" Margin="12,6">
                    <muxc:ProgressRing Grid.Row="4" IsActive="{Binding IsLoading}"/>
                    <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="16,0" Padding="24,12" CornerRadius="{StaticResource OverlayCornerRadius}" Visibility="{Binding Placeholder, Converter={StaticResource ntv}}">
                        <ctrls:FixedFontIcon Margin="0,24,0,0" FontSize="48" Glyph="{Binding Placeholder.Icon}"/>
                        <TextBlock Margin="0,10,0,0" TextAlignment="Center" TextWrapping="Wrap" Style="{StaticResource SubtitleTextBlockStyle}" Visibility="{Binding Placeholder.Header, Converter={StaticResource ntv}}" Text="{Binding Placeholder.Header}"/>
                        <TextBlock Margin="0,10,0,0" TextAlignment="Center" TextWrapping="Wrap" Style="{StaticResource BodyTextBlockStyle}" Visibility="{Binding Placeholder.Content, Converter={StaticResource ntv}}" Text="{Binding Placeholder.Content}"/>
                        <Button Margin="0,16,0,24" Tag="retry" HorizontalAlignment="Center" Style="{StaticResource AccentButtonStyle}" Visibility="{Binding Placeholder.ActionButton, Converter={StaticResource ntv}}" Content="{Binding Placeholder.ActionButton}" Command="{Binding Placeholder.ActionButtonCommand, Mode=OneWay}"/>
                    </StackPanel>
                </Grid>
            </ListView.Footer>
        </ListView>

        <StackPanel x:Name="debug" x:DeferLoadStrategy="Lazy" Grid.Row="1" Width="256" HorizontalAlignment="Right" VerticalAlignment="Bottom" IsHitTestVisible="False">
            <TextBlock FontSize="12" FontFamily="Lucida Console">
                <Run x:Name="dbgEntity" Foreground="MediumVioletRed"/>
                <LineBreak/>
                <Run x:Name="dbgOffset" Foreground="Green"/>
                <LineBreak/>
                <Run x:Name="dbgScrollable" Foreground="Green"/>
                <LineBreak/>
                <Run x:Name="dbgViewport" Foreground="Green"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</Page>