<Page
    x:Class="Elorucov.Laney.Pages.SettingsPages.Reactions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Pages.SettingsPages"
    xmlns:ctrls="using:Elorucov.Laney.Controls"
    xmlns:wuix="using:Elorucov.Laney.Controls.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Margin="24,0" MaxWidth="1048" Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Height="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <HyperlinkButton x:Name="BackButton" x:Uid="backButton" Width="36" Height="36" Margin="0,0,12,0" Padding="0" Click="BackButton_Click">
                <ctrls:FixedFontIcon FontSize="20" Glyph=""/>
            </HyperlinkButton>
            <TextBlock Grid.Column="1" Margin="0,2,0,0" Style="{StaticResource TitleTextBlockStyle}" x:Uid="settings_reactions"/>
        </Grid>
        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <wuix:SettingsGroup>
                    <wuix:SettingsExpander x:Uid="sr_most_used" IsExpanded="True" HorizontalAlignment="Stretch">
                        <Button Grid.Column="1" Click="Reset_Click" x:Uid="resetbtn"/>
                        <wuix:SettingsExpander.ItemsHeader>
                            <GridView x:Name="ReactionsButtons" Padding="0" SelectionMode="None" CanDragItems="True" AllowDrop="True" CanReorderItems="True" IsItemClickEnabled="True" ItemClick="ReactionsButtons_ItemClick">
                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Margin="6,6,4,4" Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>
                                <GridView.ItemTemplate>
                                    <DataTemplate>
                                        <Image Width="36" Height="36" Margin="1" Stretch="Uniform" DataContextChanged="Image_DataContextChanged"/>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                                <GridView.ItemContainerTransitions>
                                    <TransitionCollection>
                                        <ReorderThemeTransition/>
                                        <RepositionThemeTransition/>
                                    </TransitionCollection>
                                </GridView.ItemContainerTransitions>
                            </GridView>
                        </wuix:SettingsExpander.ItemsHeader>
                    </wuix:SettingsExpander>
                    <wuix:SettingsCard x:Uid="sr_fast_reaction">
                        <Button x:Name="FastReactionButton" MinWidth="32" Height="32" Padding="0" Click="FastReactionButton_Click" DataContextChanged="Button_DataContextChanged"/>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
