<Page
    x:Class="Elorucov.Laney.Pages.SettingsPages.Experimental"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Pages.SettingsPages"
    xmlns:ctrls="using:Elorucov.Laney.Controls"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:wuix="using:Elorucov.Laney.Controls.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Margin="24,0" MaxWidth="1048" Loaded="LoadSettings">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Height="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <HyperlinkButton x:Name="BackButton" x:Uid="backButton" Width="36" Height="36" Margin="0,0,12,0" Padding="0" Click="BackButton_Click">
                <ctrls:FixedFontIcon FontSize="20" Glyph=""/>
            </HyperlinkButton>
            <TextBlock Grid.Column="1" Margin="0,2,0,0" Style="{StaticResource TitleTextBlockStyle}" x:Uid="settings_experimental"/>
        </Grid>
        
        <ScrollViewer Grid.Row="1">
            <StackPanel Margin="0,0,0,72">
                <muxc:InfoBar x:Name="warn" IsOpen="True" IsClosable="False" Severity="Warning"/>
                <wuix:SettingsGroup Header="Common">
                    <wuix:SettingsCard x:Name="scc01" x:Uid="se_ram">
                        <wuix:SettingsCard.HeaderIcon>
                            <ctrls:FixedFontIcon Glyph=""/>
                        </wuix:SettingsCard.HeaderIcon>
                        <ToggleSwitch x:Name="pc01"/>
                    </wuix:SettingsCard>
                    <wuix:SettingsCard x:Name="scc02" x:Uid="se_entity_id">
                        <wuix:SettingsCard.HeaderIcon>
                            <ctrls:FixedFontIcon Glyph=""/>
                        </wuix:SettingsCard.HeaderIcon>
                        <ToggleSwitch x:Name="pc02"/>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
                
                <wuix:SettingsGroup Header="Interface">
                    <wuix:SettingsCard x:Name="sci01" x:Uid="se_mreb">
                        <wuix:SettingsCard.HeaderIcon>
                            <ctrls:FixedFontIcon Glyph=""/>
                        </wuix:SettingsCard.HeaderIcon>
                        <ToggleSwitch x:Name="pi01"/>
                    </wuix:SettingsCard>

                    <wuix:SettingsCard x:Name="sci03" x:Uid="se_acrylic_win11">
                        <wuix:SettingsCard.HeaderIcon>
                            <ctrls:FixedFontIcon Glyph=""/>
                        </wuix:SettingsCard.HeaderIcon>
                        <ToggleSwitch x:Name="pi03"/>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>

                <wuix:SettingsGroup Header="Network">
                    <wuix:SettingsCard x:Name="scn01" x:Uid="se_fup">
                        <wuix:SettingsCard.HeaderIcon>
                            <ctrls:FixedFontIcon Glyph=""/>
                        </wuix:SettingsCard.HeaderIcon>
                        <ComboBox x:Name="cn01">
                            <ComboBoxItem Content="Auto"/>
                            <ComboBoxItem Content="HttpClient"/>
                            <ComboBoxItem Content="BackgroundUploader"/>
                        </ComboBox>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
