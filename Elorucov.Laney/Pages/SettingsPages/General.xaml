<Page
    x:Class="Elorucov.Laney.Pages.SettingsPages.General"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Pages.SettingsPages"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:wuix="using:Elorucov.Laney.Controls.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mdl="using:Elorucov.Laney.Models"
    xmlns:ctrls="using:Elorucov.Laney.Controls"
    mc:Ignorable="d" Margin="24,0" MaxWidth="1048" Loaded="LoadSettings">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Height="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <HyperlinkButton x:Name="BackButton" x:Uid="backButton" Width="36" Height="36" Margin="0,0,12,0" Padding="0" Click="BackButton_Click">
                <ctrls:FixedFontIcon FontSize="20" Glyph=""/>
            </HyperlinkButton>
            <TextBlock Grid.Column="1" Margin="0,2,0,0" Style="{StaticResource TitleTextBlockStyle}" x:Uid="settings_general"/>
        </Grid>
        <ScrollViewer Grid.Row="1">
            <StackPanel Margin="0,0,0,16">
                <wuix:SettingsGroup>
                    <wuix:SettingsCard x:Name="lang" x:Uid="sg_lang">
                        <wuix:SettingsCard.HeaderIcon>
                            <ctrls:FixedFontIcon Glyph=""/>
                        </wuix:SettingsCard.HeaderIcon>
                        <ComboBox x:Name="CurrentLang">
                            <ComboBox.ItemTemplate>
                                <DataTemplate x:DataType="mdl:AppLanguage">
                                    <TextBlock Text="{x:Bind DisplayName}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
                <wuix:SettingsGroup x:Name="msgSendParams" x:Uid="sg_msg">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="{StaticResource ControlCornerRadius}">
                        <StackPanel>
                            <StackPanel Margin="16" Orientation="Horizontal">
                                <RadioButton x:Name="msm1" GroupName="msm" Tag="1" MinWidth="0" MaxWidth="20" Margin="0,0,16,0" Click="EnterBtnSendMethodChanged"/>
                                <StackPanel>
                                    <TextBlock x:Uid="sg_msg_1"/>
                                    <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" x:Uid="sg_msg_1_desc"/>
                                </StackPanel>
                            </StackPanel>
                            <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}"/>
                            <StackPanel Margin="16" Orientation="Horizontal">
                                <RadioButton x:Name="msm2" GroupName="msm" Tag="0" MinWidth="0" MaxWidth="20" Margin="0,0,16,0" Click="EnterBtnSendMethodChanged"/>
                                <StackPanel>
                                    <TextBlock x:Uid="sg_msg_2"/>
                                    <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" x:Uid="sg_msg_2_desc"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <wuix:SettingsCard x:Uid="sg_msg_dont_parse_links">
                        <ToggleSwitch x:Name="dontParseLinks"/>
                    </wuix:SettingsCard>
                    <wuix:SettingsCard x:Uid="sg_msg_disable_mentions">
                        <ToggleSwitch x:Name="disableMentions"/>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
                <wuix:SettingsGroup x:Name="stickers" x:Uid="sg_stickers">
                    <wuix:SettingsCard x:Uid="sg_stickers_suggestions">
                        <ToggleSwitch x:Name="stickersSuggestions" Toggled="EnableDisableStickersSuggestions"/>
                    </wuix:SettingsCard>
                    <wuix:SettingsCard x:Name="animStickersStg" x:Uid="sg_stickers_animation">
                        <ToggleSwitch x:Name="AnimatedStickers" Toggled="EnableDisableAnimatedStickers"/>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
                <wuix:SettingsGroup x:Name="passcode" x:Uid="sg_passcode">
                    <wuix:SettingsCard x:Uid="sg_passcode">
                        <wuix:SettingsCard.HeaderIcon>
                            <ctrls:FixedFontIcon Glyph=""/>
                        </wuix:SettingsCard.HeaderIcon>
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="passetbtn" x:Uid="sg_passcode_set" Click="SetOrChangePasscode"/>
                            <Button x:Name="passrmbtn" x:Uid="sg_passcode_remove" Margin="2,0,0,0" Visibility="Collapsed" Click="RemovePasscode"/>
                        </StackPanel>
                    </wuix:SettingsCard>
                    <wuix:SettingsCard x:Name="passwinhello" Visibility="Collapsed" x:Uid="sg_pass_winhello">
                        <wuix:SettingsCard.HeaderIcon>
                            <ctrls:FixedFontIcon Glyph=""/>
                        </wuix:SettingsCard.HeaderIcon>
                        <ToggleSwitch IsEnabled="False" Toggled="EnableDisableWinHello"/>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
                <wuix:SettingsGroup x:Name="adv" x:Uid="sg_advanced">
                    <wuix:SettingsCard x:Name="textSelection" x:Uid="sg_text_selection">
                        <ToggleSwitch x:Name="p13"/>
                    </wuix:SettingsCard>
                    <wuix:SettingsCard x:Name="lpfix" x:Uid="sg_lp_fix">
                        <ToggleSwitch x:Name="p12"/>
                    </wuix:SettingsCard>
                    <wuix:SettingsCard x:Name="loginfo" x:Uid="sg_logs">
                        <StackPanel Orientation="Horizontal">
                            <muxc:ProgressRing x:Name="prog" Width="32" Height="32" Margin="0,0,12,0" IsActive="True"/>
                            <Button x:Name="logbtn" IsEnabled="False" x:Uid="sg_logs_export" Click="ExportLogs"/>
                        </StackPanel>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>