<Page
    x:Class="Elorucov.Laney.Pages.SettingsPages.Blacklist"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Pages.SettingsPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrls="using:Elorucov.Laney.Controls"
    xmlns:elor="using:Elorucov.Toolkit.UWP.Controls"
    xmlns:mxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:obj="using:Elorucov.Laney.Models"
    mc:Ignorable="d" Margin="24,0" MaxWidth="1048" Loaded="Load">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Height="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <HyperlinkButton x:Name="BackButton" x:Uid="backButton" Width="36" Height="36" Margin="0,0,12,0" Padding="0" Click="BackButton_Click">
                <ctrls:FixedFontIcon FontSize="20" Glyph=""/>
            </HyperlinkButton>
            <TextBlock Grid.Column="1" Margin="0,2,0,0" Style="{StaticResource TitleTextBlockStyle}" x:Uid="settings_blacklist"/>
        </Grid>
        <ListView x:Name="list" Grid.Row="1" SelectionMode="None">
            <ListView.Header>
                <TextBlock x:Name="loadStatus" TextWrapping="Wrap"/>
            </ListView.Header>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="obj:MentionItem">
                    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="{StaticResource ControlCornerRadius}" Padding="12" Margin="0,0,0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <elor:Avatar IsHitTestVisible="False" Width="32" Height="32" ImageUri="{x:Bind Avatar}" DisplayName="{x:Bind Name}"/>
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0" Text="{x:Bind Name}"/>
                        <HyperlinkButton Grid.Column="2" Width="32" Height="32" Padding="0" Click="RemoveFromBlacklist">
                            <ctrls:FixedFontIcon Glyph="&#xE106;"/>
                        </HyperlinkButton>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <mxc:ProgressRing x:Name="progress" HorizontalAlignment="Left" VerticalAlignment="Top" IsIndeterminate="False" IsHitTestVisible="False" Width="20" Height="20"/>
    </Grid>
</Page>
