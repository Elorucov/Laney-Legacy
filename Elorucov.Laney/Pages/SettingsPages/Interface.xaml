<Page
    x:Class="Elorucov.Laney.Pages.SettingsPages.Interface"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Pages.SettingsPages"
    xmlns:wuix="using:Elorucov.Laney.Controls.WinUI"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:mdl="using:Elorucov.Laney.Models"
    xmlns:uix="using:Elorucov.Laney.Models.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrls="using:Elorucov.Laney.Controls"
    mc:Ignorable="d" MaxWidth="1048">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Height="48" VerticalAlignment="Top" Margin="24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <HyperlinkButton x:Name="BackButton" x:Uid="backButton" Width="36" Height="36" Margin="0,0,12,0" Padding="0" Click="BackButton_Click">
                <ctrls:FixedFontIcon FontSize="20" Glyph=""/>
            </HyperlinkButton>
            <TextBlock Grid.Column="1" Margin="0,2,0,0" Style="{StaticResource TitleTextBlockStyle}" x:Uid="settings_appearance"/>
        </Grid>

        <ScrollViewer Grid.Row="1">
            <StackPanel Margin="0,0,0,16">
                <Grid x:Name="demoUI" IsHitTestVisible="False" Height="350" Margin="0,0,0,24" Background="{ThemeResource VKBackgroundContentBrush}" CornerRadius="4">
                    <ctrls:ChatBackgroundControl x:Name="ChatBackground"/>
                    <ctrls:ChatListView x:Name="demoMessagesList" SelectionMode="None" Tag="demo" ItemsSource="{Binding Messages}">
                        <ctrls:ChatListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Background" Value="Transparent"/>
                            </Style>
                        </ctrls:ChatListView.ItemContainerStyle>
                        <ctrls:ChatListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsStackPanel AreStickyGroupHeadersEnabled="False" GroupPadding="0" ItemsUpdatingScrollMode="KeepLastItemInView" VerticalAlignment="Bottom" CacheLength="10"/>
                            </ItemsPanelTemplate>
                        </ctrls:ChatListView.ItemsPanel>
                        <ctrls:ChatListView.ItemTemplate>
                            <DataTemplate x:DataType="mdl:LMessage">
                                <muxc:SwipeControl MinHeight="32"/>
                            </DataTemplate>
                        </ctrls:ChatListView.ItemTemplate>
                    </ctrls:ChatListView>
                </Grid>

                <wuix:SettingsGroup x:Name="SettingItems" Margin="24,0">
                    <wuix:SettingsCard x:Name="theme" x:Uid="sa_theme">
                        <wuix:SettingsCard.HeaderIcon>
                            <ctrls:FixedFontIcon Glyph=""/>
                        </wuix:SettingsCard.HeaderIcon>
                        <ComboBox x:Name="p04" Grid.Column="1" HorizontalAlignment="Stretch">
                            <ComboBoxItem x:Uid="sa_theme_sys"/>
                            <ComboBoxItem x:Uid="sa_theme_light"/>
                            <ComboBoxItem x:Uid="sa_theme_dark"/>
                        </ComboBox>
                    </wuix:SettingsCard>
                    <wuix:SettingsCard x:Name="cteStyle" x:Uid="sa_style">
                        <wuix:SettingsCard.HeaderIcon>
                            <ctrls:FixedFontIcon Glyph=""/>
                        </wuix:SettingsCard.HeaderIcon>
                        <ComboBox x:Name="p07" Grid.Column="1" HorizontalAlignment="Stretch"/>
                    </wuix:SettingsCard>
                    <wuix:SettingsExpander x:Name="BackgroundsExpander" x:Uid="sa_background" Visibility="Collapsed" IsExpanded="True" HorizontalAlignment="Stretch">
                        <Button Grid.Column="1" Click="ChooseFileForBackground">
                            <StackPanel Orientation="Horizontal">
                                <ctrls:FixedFontIcon Glyph="" Margin="0,0,5,0"/>
                                <TextBlock x:Uid="sa_bkg_file"/>
                            </StackPanel>
                        </Button>
                        <wuix:SettingsExpander.ItemsHeader>
                            <GridView x:Name="BackgroundsList" SelectionMode="Single" Margin="16,16,0,0">
                                <GridView.ItemTemplate>
                                    <DataTemplate x:DataType="uix:Background">
                                        <Border Width="78" Height="78" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4" ToolTipService.ToolTip="{x:Bind Id}">
                                            <ctrls:ChatBackgroundControl ChatBackground="{x:Bind}" VectorScale="0.5"/>
                                        </Border>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                        </wuix:SettingsExpander.ItemsHeader>
                    </wuix:SettingsExpander>
                    <wuix:SettingsExpander x:Name="ColorsExpander" x:Uid="sa_color" Visibility="Collapsed" IsExpanded="True" HorizontalAlignment="Stretch">
                        <wuix:SettingsExpander.ItemsHeader>
                            <GridView x:Name="ColorsList" SelectionMode="Single" Margin="16,16,0,0">
                                <GridView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Width="36" Height="36" CornerRadius="4" Background="{ThemeResource VKImBubbleOutgoingBrush}">
                                            <Border Width="36" Height="36" CornerRadius="4" DataContextChanged="DrawGradient"/>
                                        </Grid>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                        </wuix:SettingsExpander.ItemsHeader>
                    </wuix:SettingsExpander>
                    <wuix:SettingsCard x:Name="cteIgnoreChatThemeContainer" x:Uid="sa_ignore_chat_theme">
                        <ToggleSwitch x:Name="p08"/>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
                <wuix:SettingsGroup x:Uid="sa_messages_ui" Margin="24,0">
                    <wuix:SettingsCard x:Uid="sa_previews_position">
                        <ComboBox x:Name="p11" Grid.Column="1" HorizontalAlignment="Stretch">
                            <ComboBoxItem x:Uid="sa_previews_position_below"/>
                            <ComboBoxItem x:Uid="sa_previews_position_above"/>
                        </ComboBox>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
                <wuix:SettingsGroup x:Uid="sa_clist_lines" Margin="24,0">
                    <wuix:SettingsCard x:Uid="sa_clist_lines">
                        <ComboBox x:Name="p09" Grid.Column="1" HorizontalAlignment="Stretch">
                            <ComboBoxItem x:Uid="sa_clist_lines_2"/>
                            <ComboBoxItem x:Uid="sa_clist_lines_3"/>
                        </ComboBox>
                    </wuix:SettingsCard>
                    <wuix:SettingsCard x:Uid="sa_last_message_preview">
                        <ToggleSwitch x:Name="p10"/>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
                <wuix:SettingsGroup x:Uid="sa_font" Margin="24,0">
                    <wuix:SettingsCard x:Name="altEmojiFontSetting" x:Uid="sa_emoji">
                        <wuix:SettingsCard.HeaderIcon>
                            <ctrls:FixedFontIcon Glyph=""/>
                        </wuix:SettingsCard.HeaderIcon>
                        <ToggleSwitch x:Name="p03"/>
                    </wuix:SettingsCard>
                    <wuix:SettingsCard x:Uid="sa_textsize">
                        <wuix:SettingsCard.HeaderIcon>
                            <ctrls:FixedFontIcon Glyph=""/>
                        </wuix:SettingsCard.HeaderIcon>
                        <muxc:NumberBox x:Name="p06" SpinButtonPlacementMode="Compact" Minimum="14" Maximum="17"/>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
                <wuix:SettingsGroup x:Name="xb" Visibility="Collapsed" Header="Xbox" Margin="24,0">
                    <wuix:SettingsCard Margin="0,0,0,3" Header="UI scaling">
                        <ToggleSwitch x:Name="p01"/>
                    </wuix:SettingsCard>
                    <wuix:SettingsCard Margin="0,0,0,3" Header="Drawing UI" Description="to the edge of the screen">
                        <ToggleSwitch x:Name="p02"/>
                    </wuix:SettingsCard>
                </wuix:SettingsGroup>
                <Grid x:Name="accentDemo" Height="8" CornerRadius="4" Margin="24,12,24,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0">
                        <Border.Background>
                            <SolidColorBrush Color="{ThemeResource SystemAccentColorDark3}"/>
                        </Border.Background>
                    </Border>
                    <Border Grid.Column="1">
                        <Border.Background>
                            <SolidColorBrush Color="{ThemeResource SystemAccentColorDark2}"/>
                        </Border.Background>
                    </Border>
                    <Border Grid.Column="2">
                        <Border.Background>
                            <SolidColorBrush Color="{ThemeResource SystemAccentColorDark1}"/>
                        </Border.Background>
                    </Border>
                    <Border Grid.Column="3">
                        <Border.Background>
                            <SolidColorBrush Color="{ThemeResource SystemAccentColor}"/>
                        </Border.Background>
                    </Border>
                    <Border Grid.Column="4">
                        <Border.Background>
                            <SolidColorBrush Color="{ThemeResource SystemAccentColorLight1}"/>
                        </Border.Background>
                    </Border>
                    <Border Grid.Column="5">
                        <Border.Background>
                            <SolidColorBrush Color="{ThemeResource SystemAccentColorLight2}"/>
                        </Border.Background>
                    </Border>
                    <Border Grid.Column="6">
                        <Border.Background>
                            <SolidColorBrush Color="{ThemeResource SystemAccentColorLight3}"/>
                        </Border.Background>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>