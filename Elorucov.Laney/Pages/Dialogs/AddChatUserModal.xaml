<elor:Modal
    x:Class="Elorucov.Laney.Pages.Dialogs.AddChatUserModal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:elor="using:Elorucov.Toolkit.UWP.Controls"
    xmlns:vkobj="using:Elorucov.VkAPI.Objects"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Style="{StaticResource ContentUnderTitleBarModalStyle}"
    MaxWidth="576" x:Uid="addchatuser_modal" CloseButtonVisibility="Visible" FullSizeDesired="True" Loaded="LoadFriends">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Background="{ThemeResource LayerFillColorAltBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1"/>
        <ListView x:Name="FriendsList" Margin="0,48,0,0" SelectionMode="Multiple" SelectionChanged="FriendsList_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vkobj:User">
                    <Grid Margin="0,6,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <elor:Avatar Width="42" Height="42" Margin="0,0,12,0" ImageUri="{x:Bind Photo, Mode=OneWay}" DisplayName="{x:Bind FullName}"/>
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{x:Bind FullName}" FontWeight="SemiBold" TextLineBounds="TrimToBaseline" FontSize="16">
                            <TextBlock.RenderTransform>
                                <CompositeTransform TranslateY="-3"/>
                            </TextBlock.RenderTransform>
                        </TextBlock>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid x:Name="ModalControls" Grid.Row="1" Height="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="NextBtn" Grid.Column="1" Margin="8" VerticalAlignment="Center" IsEnabled="False" Style="{StaticResource AccentButtonStyle}" x:Uid="nextbtn">
                <Button.Flyout>
                    <Flyout Placement="Top">
                        <StackPanel Margin="12,9,12,12">
                            <TextBlock FontWeight="SemiBold" x:Uid="addchatuser_vmc_1"/>
                            <StackPanel Margin="0,0,0,12" Orientation="Horizontal" HorizontalAlignment="Left">
                                <TextBox x:Name="VisibleMessagesCount" VerticalAlignment="Top" MaxLength="4" Margin="0,0,6,0" Padding="4,0" InputScope="Digits" Width="36" TextWrapping="Wrap" Text="0"/>
                                <TextBlock Margin="8,0,0,0" VerticalAlignment="Top" FontWeight="SemiBold" x:Uid="addchatuser_vmc_2"/>
                            </StackPanel>
                            <TextBlock x:Name="ErrText" Margin="0,0,0,12" Foreground="Red" Visibility="Collapsed"/>
                            <Button Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" x:Uid="invitebtn" Click="AddChatBtn"/>
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
            </Button>
        </Grid>
    </Grid>
</elor:Modal>
