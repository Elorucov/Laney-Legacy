<elor:Modal
    x:Class="Elorucov.Laney.Pages.Dialogs.Dev.MessageJSONView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Pages.Dialogs.Dev"
    xmlns:elor="using:Elorucov.Toolkit.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Style="{StaticResource ContentUnderTitleBarModalStyle}"
    FullSizeDesired="True" MaxWidth="576" DropShadow="True" Title="Message JSON" Loaded="GetMessageJSON">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.ColumnSpan="2" Background="{ThemeResource LayerFillColorAltBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <Grid.Resources>
                <SolidColorBrush x:Key="TextControlBackgroundDisabled" Color="Transparent"/>
                <SolidColorBrush x:Key="TextControlBorderBrushDisabled" Color="Transparent"/>
                <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="Transparent"/>
                <SolidColorBrush x:Key="TextControlBackgroundFocused" Color="Transparent"/>
                <SolidColorBrush x:Key="TextControlBorderBrushFocused" Color="Transparent"/>
            </Grid.Resources>
            <TextBox x:Name="JSONCode" Margin="12,48,12,0" Background="Transparent" IsReadOnly="True" IsSpellCheckEnabled="False" BorderThickness="0" TextWrapping="Wrap" Padding="0" FontFamily="Lucida Console" FontSize="12"/>
        </Grid>
        <StackPanel Margin="6" Grid.Row="1" HorizontalAlignment="Left" Orientation="Horizontal">
            <TextBox x:Name="APIVersion" Grid.Row="1" PlaceholderText="API version" Margin="6"/>
            <CheckBox x:Name="wtcb" Visibility="Collapsed" Margin="6" Content="As official client"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Margin="6" Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Margin="6" Content="Refresh" Click="Resend"/>
            <Button Margin="6" Content="Copy" Click="Copy"/>
        </StackPanel>
    </Grid>
</elor:Modal>
