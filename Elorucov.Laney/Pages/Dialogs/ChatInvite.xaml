<elor:Modal
    x:Class="Elorucov.Laney.Pages.Dialogs.ChatInvite"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Pages.Dialogs"
    xmlns:elor="using:Elorucov.Toolkit.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" CloseButtonVisibility="Collapsed" Style="{StaticResource ContentUnderTitleBarModalStyle}"
    Background="{ThemeResource VKModalCardBackgroundBrush}" Loaded="GetChatPreview"
    MaxWidth="420" Margin="8" Padding="0" DropShadow="True" BorderThickness="1" BorderBrush="{ThemeResource VKModalCardBorderBrush}">

    <Grid>
        <Grid x:Name="MainView" Padding="16">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel>
                <Ellipse x:Name="ChatPhoto" Width="80" Height="80" Margin="0,8,0,0">
                    <Ellipse.Fill>
                        <SolidColorBrush Color="#7f7f7f"/>
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock x:Name="Name" Margin="0,14,0,0" LineHeight="28" TextWrapping="Wrap" FontSize="20" FontWeight="SemiBold" MaxLines="3" TextAlignment="Center"/>
                <TextBlock x:Name="Subtitle" Margin="0,6,0,0" FontSize="15" TextWrapping="Wrap" MaxLines="3" TextAlignment="Center" Opacity="0.6"/>
                <elor:UserAvatars x:Name="MembersAvatars" Margin="0,24,0,0" HorizontalAlignment="Center"/>
                <TextBlock x:Name="MembersNames" Margin="0,6,0,0" FontSize="15" TextWrapping="Wrap" MaxLines="3" TextAlignment="Center" Opacity="0.6"/>
            </StackPanel>
            <Button x:Name="JoinBtn" Grid.Row="1" HorizontalAlignment="Stretch" Margin="0,24,0,0" Style="{StaticResource VKButtonPrimaryExtraLarge}" Click="JoinChat"/>
        </Grid>
        <HyperlinkButton Margin="4" VerticalAlignment="Top" HorizontalAlignment="Right" Padding="0" BorderThickness="0" Width="48" Height="48" Click="Close">
            <ContentPresenter Width="24" Height="24" Foreground="{ThemeResource VKButtonPrimaryBackgroundBrush}" VerticalAlignment="Center" HorizontalAlignment="Center" ContentTemplate="{StaticResource Icon24Dismiss}"/>
        </HyperlinkButton>
    </Grid>
</elor:Modal>