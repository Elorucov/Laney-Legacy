<elor:Modal
    x:Class="Elorucov.Laney.Pages.Dialogs.ImportantMessages"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Pages.Dialogs"
    xmlns:mxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:elor="using:Elorucov.Toolkit.UWP.Controls"
    xmlns:converters="using:Elorucov.Laney.Services.Converters.MessageItem"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Style="{StaticResource ContentUnderTitleBarModalStyle}"
    FullSizeDesired="True" Padding="0" DropShadow="True" MaxWidth="576" x:Uid="important_msgs_modal" CloseButtonVisibility="Visible" Loaded="StartLoadMessages">

    <elor:Modal.Resources>
        <converters:NullToVisibility x:Key="ntv"/>
        <converters:NullToVisibility2 x:Key="ntv2"/>
        <converters:DateTimeToString x:Key="dt"/>
    </elor:Modal.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="{ThemeResource LayerFillColorAltBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1"/>
        <ListView x:Name="listView" Margin="0,48,0,0"  SelectionMode="None" IsItemClickEnabled="True">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel x:Name="isp"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Margin="-12,6" Background="Transparent" ContextRequested="ShowMsgContextMenu"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal" Margin="6">
            <TextBlock x:Uid="offstxt" Margin="6, 12, 6, 6"/>
            <TextBox x:Name="offset" Margin="6" Width="92" InputScope="Number" KeyDown="offsetKeyDown"/>
            <Button x:Uid="gobtn" Margin="6" Style="{StaticResource AccentButtonStyle}" Click="LoadWithOffset"/>
        </StackPanel>
        <mxc:ProgressRing x:Name="progress" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center" Width="24" Height="24" Margin="10" IsIndeterminate="False"/>
    </Grid>
</elor:Modal>
