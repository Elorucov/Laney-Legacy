<elor:Modal
    x:Class="Elorucov.Laney.Pages.Dialogs.StickerPackPreviewModal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Pages.Dialogs"
    xmlns:elor="using:Elorucov.Toolkit.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" x:Uid="sticker_pack" Style="{StaticResource ContentUnderTitleBarModalStyle}" Padding="0"
    FullSizeDesired="True" DropShadow="True" MaxWidth="480">

    <Grid SizeChanged="OnSizeChanged">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Background="{ThemeResource LayerFillColorAltBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <Border x:Name="TitleBar" Height="48"/>
            <Grid x:Name="PackInfo" Height="86" Margin="12,0,12,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Rectangle x:Name="PackBackground" Width="86" Height="86" RadiusX="10" RadiusY="10" Margin="0,0,12,0" Fill="{ThemeResource VKPlaceholderIconBackgroundBrush}"/>
                <Image x:Name="PackPhoto" HorizontalAlignment="Left" Width="72" Height="72" Margin="7" Stretch="Uniform"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="PackTitle" Style="{StaticResource VKHeadline}" TextWrapping="Wrap" MaxLines="2" FontWeight="SemiBold"/>
                    <TextBlock x:Name="PackAuthor" Style="{StaticResource VKSubhead}" TextWrapping="Wrap" MaxLines="2" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
            <TextBlock x:Name="PackDescription" TextWrapping="Wrap" Margin="12,0,12,12"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1">
            <VariableSizedWrapGrid x:Name="StickersContainer" HorizontalAlignment="Center" MaximumRowsOrColumns="4" Orientation="Horizontal"/>
        </ScrollViewer>
        <Border Grid.Row="1" VerticalAlignment="Bottom" Height="24" Background="{ThemeResource GradientUnderModalButtonBrush}"/>
        <Button x:Uid="open_in_browser" Grid.Row="2" Margin="12,0,12,12" HorizontalAlignment="Stretch" Style="{StaticResource AccentButtonStyle}" Click="OpenInBrowser"/>
    </Grid>
</elor:Modal>