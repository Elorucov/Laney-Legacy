<elor:Modal
    x:Class="Elorucov.Laney.Pages.Dialogs.ChatSettingsModal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Pages.Dialogs"
    xmlns:elor="using:Elorucov.Toolkit.UWP.Controls"
    xmlns:mdl="using:Elorucov.Laney.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    MaxWidth="480" Padding="0" Style="{StaticResource ContentUnderTitleBarModalStyle}" x:Uid="chatmgmt">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.RowSpan="3" Background="{ThemeResource LayerFillColorAltBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1"/>

        <Border Height="42"/>
        <Grid x:Name="Templates" x:DeferLoadStrategy="Lazy" Grid.Row="1" Margin="6,6,6,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="6" HorizontalAlignment="Stretch" x:Uid="chatmgmt_chat_regular" Click="SetupForOpen"/>
            <Button Grid.Column="1" Margin="6" HorizontalAlignment="Stretch" x:Uid="chatmgmt_chat_private" Click="SetupForClosed"/>
        </Grid>
        <ListView x:Name="SettingsList" Grid.Row="2" SelectionMode="None" IsItemClickEnabled="True" ItemClick="ShowOptionsContextMenu">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="mdl:ChatSettingsItem">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" ContentTemplate="{x:Bind Icon}" Margin="12" Width="28" Height="28"/>
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="0,-19,0,0" FontSize="15" TextTrimming="CharacterEllipsis" Text="{x:Bind Title}"/>
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="0,18,0,0" FontSize="13" TextTrimming="CharacterEllipsis" Foreground="{ThemeResource VKTextSecondaryBrush}" Text="{x:Bind Subtitle, Mode=OneWay}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
        <StackPanel x:Name="Footer" x:DeferLoadStrategy="Lazy" Grid.Row="3" Padding="6" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Margin="6" Style="{StaticResource AccentButtonStyle}" x:Uid="polleditor_save" Click="SaveSettings"/>
        </StackPanel>
    </Grid>
</elor:Modal>