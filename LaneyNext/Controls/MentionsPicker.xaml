<UserControl
    x:Class="Elorucov.Laney.Controls.MentionsPicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Controls"
    xmlns:data="using:Elorucov.Laney.DataModels"
    xmlns:elor="using:Elorucov.Toolkit.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="Transparent" Tapped="Hide">
        <ListView x:Name="mentionslist" VerticalAlignment="Bottom" MaxHeight="236" ItemContainerTransitions="{x:Null}" Background="{ThemeResource VKBackgroundContentBrush}" BorderBrush="#7f7f7f7f" BorderThickness="0,1,0,0" SelectionMode="None" IsItemClickEnabled="True" ItemClick="Mentionslist_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Entity">
                    <Grid Margin="0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <elor:Avatar Width="40" Height="40" ImageUri="{x:Bind Image}" DisplayName="{x:Bind Title}"/>
                        <StackPanel Grid.Column="1" Margin="10,0,0,2" VerticalAlignment="Center">
                            <TextBlock x:Name="TitleString" FontSize="15" MaxLines="1" Text="{x:Bind Title}"/>
                            <TextBlock x:Name="DescString" Margin="0,0,0,0" FontSize="14" FontWeight="SemiLight" MaxLines="1" Opacity="0.5" Text="{x:Bind Subtitle}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
