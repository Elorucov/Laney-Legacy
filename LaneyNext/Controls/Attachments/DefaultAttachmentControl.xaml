<UserControl
    x:Class="Elorucov.Laney.Controls.Attachments.DefaultAttachmentControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Controls.Attachments"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Style="{StaticResource TransparentButtonStyle}" Padding="0" Height="40" Click="HandleClick" >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border Height="40" Width="40" CornerRadius="20" Background="{ThemeResource VKButtonPrimaryBackgroundBrush}">
                <ContentPresenter x:Name="IconPresenter" Height="24" Width="24" Foreground="{ThemeResource VKImBubbleOutgoingBrush}" ContentTemplate="{StaticResource Icon24LogoVk}"/>
            </Border>
            <StackPanel Grid.Column="1" Margin="10,0,0,2" VerticalAlignment="Center">
                <TextBlock x:Name="TitleString" FontSize="15" FontWeight="SemiBold" MaxLines="1" Text="Title"/>
                <TextBlock x:Name="DescString" Margin="0,0,0,0" FontSize="14" Foreground="{ThemeResource VKTextSecondaryBrush}" MaxLines="1" Text="Description"/>
            </StackPanel>
        </Grid>
    </Button>
</UserControl>
