<UserControl
    x:Class="Elorucov.Laney.Controls.Attachments.StickerPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Controls.Attachments"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Elorucov.Laney.Controls"
    mc:Ignorable="d"
    d:DesignHeight="168"
    d:DesignWidth="168">

    <Button HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Padding="0" Style="{StaticResource TransparentButtonStyle}">
        <Grid>
            <Image x:Name="StaticSticker" Stretch="UniformToFill" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Image.Source>
                    <BitmapImage x:Name="img" DecodePixelType="Logical"/>
                </Image.Source>
            </Image>
        </Grid>
    </Button>
</UserControl>
