<UserControl
    x:Class="Elorucov.Laney.Controls.Attachments.VoiceMessageControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Controls.Attachments"
    xmlns:vkui="using:VK.VKUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Height="40">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Button Style="{StaticResource EllipseButtonStyle}" Background="Transparent" Padding="0" Click="PlayPauseButtonClicked">
            <Border Height="40" Width="40" CornerRadius="20" BorderThickness="2" BorderBrush="{ThemeResource VKButtonPrimaryBackgroundBrush}">
                <ContentPresenter x:Name="IconPresenter" Height="24" Width="24" Foreground="{ThemeResource VKButtonPrimaryBackgroundBrush}" ContentTemplate="{StaticResource Icon24Play}"/>
            </Border>
        </Button>
        <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
            <Grid x:Name="WaveContainer" Height="26">
                <Canvas x:Name="BackgroundSoundWave" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Opacity="0.5"/>
                <Canvas x:Name="ForegroundSoundWave" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                <Border x:Name="Seeker" Background="Transparent" PointerPressed="Seeker_PointerPressed"/>
            </Grid>
            <TextBlock x:Name="Duration" Margin="0,0,0,0" FontSize="12" Foreground="{ThemeResource VKButtonPrimaryBackgroundBrush}" MaxLines="1" Text="-:--"/>
        </StackPanel>
        <Button x:Name="TranscriptButton" Visibility="Collapsed" Grid.Column="2" Width="36" Height="26" Margin="8,0,0,0" Padding="0" VerticalAlignment="Top" Style="{StaticResource VKButtonSecondaryMedium}" Click="ShowTranscript">
            <vkui:VKIcon Id="Icon24TextOutline" Margin="1" Width="24" Height="24"/>
        </Button>
    </Grid>
</UserControl>
