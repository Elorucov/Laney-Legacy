<UserControl
    x:Class="Elorucov.Laney.Controls.Attachments.AudioControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Controls.Attachments"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Height="40">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Button Style="{StaticResource TransparentButtonStyle}" BorderThickness="0" Padding="0" Click="PlayPauseClicked">
            <Border Height="40" Width="40" CornerRadius="20" Background="{ThemeResource VKButtonPrimaryBackgroundBrush}">
                <ContentPresenter x:Name="IconPresenter" Height="24" Width="24" Foreground="{ThemeResource VKImBubbleOutgoingBrush}" ContentTemplate="{StaticResource Icon24Play}"/>
            </Border>
        </Button>

        <StackPanel x:Name="AudioInfo" Grid.Column="1" Margin="10,0,0,2" VerticalAlignment="Center">
            <TextBlock FontSize="15" MaxLines="1" >
                <Run x:Name="SongName" FontWeight="SemiBold"/>
                <Run x:Name="SongSubtitle" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
            </TextBlock>
            <TextBlock x:Name="SongArtist" Margin="0,0,0,0" FontSize="14" Foreground="{ThemeResource VKTextSecondaryBrush}" MaxLines="1"/>
        </StackPanel>

        <TextBlock x:Name="Duration" Grid.Column="2" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="{ThemeResource VKTextSecondaryBrush}" FontSize="14" Text="-:--"/>
    </Grid>
</UserControl>
