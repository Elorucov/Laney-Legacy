<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.DataTemplates">
    <ControlTemplate x:Key="ServiceMessageTemplate" TargetType="ContentControl">
        <Border Margin="0,4" CornerRadius="12" HorizontalAlignment="Center" Background="{ThemeResource VKBackgroundContentBrush}">
            <ContentPresenter FontSize="12" Margin="8,3,8,5" Foreground="{ThemeResource VKTextPrimaryBrush}" Content="{TemplateBinding Content}"/>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="TimeInBubbleImageTemplate" TargetType="ContentControl">
        <Border CornerRadius="12" HorizontalAlignment="Center" Background="#80000000" RequestedTheme="Dark">
            <ContentPresenter FontSize="12" Margin="8,4,8,4" Content="{TemplateBinding Content}"/>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="SelfDestructionTimerTemplate" TargetType="ContentControl">
        <Grid>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch" CornerRadius="10" Padding="6,3,6,4" Background="{ThemeResource VKBackgroundContentBrush}" BorderBrush="#3f000000" BorderThickness="1">
                <ContentPresenter Width="14" Height="14" ContentTemplate="{StaticResource Icon28BombOutline}">
                    <ContentPresenter.Foreground>
                        <SolidColorBrush x:Name="IconForeground" Color="Gray"/>
                    </ContentPresenter.Foreground>
                    <ContentPresenter.Triggers>
                        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                            <EventTrigger.Actions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="IconForeground" Storyboard.TargetProperty="Color" From="Red" To="Gray" Duration="0:0:0.5" RepeatBehavior="Forever" AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger.Actions>
                        </EventTrigger>
                    </ContentPresenter.Triggers>
                </ContentPresenter>
                <ContentPresenter FontSize="12" Margin="4,0,0,0" Content="{TemplateBinding Content}" Foreground="{ThemeResource VKIconOutlineMediumBrush}" Visibility="{Binding IsChecked, ElementName=Switcher}"/>
            </StackPanel>
            <ToggleButton x:Name="Switcher" Opacity="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        </Grid>
    </ControlTemplate>

    <DataTemplate x:Key="DisappearedMessageTemplate">
        <Button HorizontalAlignment="Center" Style="{StaticResource TransparentButtonStyle}" Padding="0" Margin="4">
            <StackPanel CornerRadius="16" Height="32" Orientation="Horizontal" Padding="6,0,8,0" Background="Transparent" BorderBrush="{ThemeResource VKImBubbleOutgoingBrush}" BorderThickness="1">
                <ContentPresenter Width="20" Height="20" Margin="0,0,6,0" VerticalAlignment="Center" ContentTemplate="{StaticResource Icon28BombOutline}" Foreground="{ThemeResource VKTextSecondaryBrush}"/>
                <TextBlock FontSize="13" Margin="0,-2,0,0" VerticalAlignment="Center" Foreground="{ThemeResource VKTextSecondaryBrush}" Text="{Binding Converter={StaticResource loc}, ConverterParameter=msg_disappeared}"/>
            </StackPanel>
        </Button>
    </DataTemplate>
</ResourceDictionary>
