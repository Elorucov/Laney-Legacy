<Page
    x:Class="Elorucov.Laney.Views.SearchView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Views"
    xmlns:vkui="using:VK.VKUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource VKBackgroundContentBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <vkui:PageHeader Padding="0" BorderBrush="Transparent">
            <vkui:PageHeader.LeftButtons>
                <vkui:PageHeaderButton Icon="Icon28ArrowLeftOutline" Click="GoBack"/>
            </vkui:PageHeader.LeftButtons>
            <TextBox x:Name="SearchBox" Style="{StaticResource VKSearchBar}" InputScope="Search" Margin="0,8" PlaceholderText="{x:Bind Converter={StaticResource loc}, ConverterParameter=search}" Text="{Binding Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" KeyDown="SearchBoxKeyDown"/>
        </vkui:PageHeader>
        <Pivot Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}">
            <PivotItem Margin="0" Header="{x:Bind Converter={StaticResource loc}, ConverterParameter=search_conv}">
                <Grid DataContext="{Binding FoundConversations}">
                    <ListView VerticalAlignment="Top" SelectionMode="None" ItemsSource="{Binding Items}" ItemTemplate="{StaticResource ConversationDataTemplateLite}" ItemContainerStyle="{StaticResource CommonListViewItemStyle}" IsItemClickEnabled="True" ItemClick="ClickedOnConversation">
                        <ListView.Footer>
                            <Border MinHeight="8">
                                <vkui:Spinner VerticalAlignment="Center" Width="24" Height="24" Margin="12" Visibility="{Binding IsLoading}" Foreground="{ThemeResource VKHeaderAlternateTabActiveIndicatorBrush}"/>
                            </Border>
                        </ListView.Footer>
                    </ListView>
                    <vkui:Placeholder Grid.ColumnSpan="2" VerticalAlignment="Stretch" 
                          Visibility="{Binding Placeholder, Converter={StaticResource ntv}}"
                          IconTemplate="{Binding Placeholder, Converter={StaticResource pic}}" 
                          Header="{Binding Placeholder.Header}"
                          Content="{Binding Placeholder.Content}"
                          ActionButtonText="{Binding Placeholder.ActionButton}"
                          ActionButtonCommand="{Binding Placeholder.ActionButtonCommand}"/>
                </Grid>
            </PivotItem>
            <PivotItem Margin="0" Header="{x:Bind Converter={StaticResource loc}, ConverterParameter=search_messages}">
                <Grid DataContext="{Binding FoundMessages}">
                    <ListView VerticalAlignment="Top" SelectionMode="None" ItemsSource="{Binding Items}" ItemTemplate="{StaticResource FoundMessageDataTemplate}" ItemContainerStyle="{StaticResource CommonListViewItemStyle}" IsItemClickEnabled="True" ItemClick="ClickedOnMessage" Loaded="InitScrollViewer">
                        <ListView.Footer>
                            <Border MinHeight="8">
                                <vkui:Spinner VerticalAlignment="Center" Width="24" Height="24" Margin="12" Visibility="{Binding IsLoading}" Foreground="{ThemeResource VKHeaderAlternateTabActiveIndicatorBrush}"/>
                            </Border>
                        </ListView.Footer>
                    </ListView>
                    <vkui:Placeholder Grid.ColumnSpan="2" VerticalAlignment="Stretch" 
                          Visibility="{Binding Placeholder, Converter={StaticResource ntv}}"
                          IconTemplate="{Binding Placeholder, Converter={StaticResource pic}}" 
                          Header="{Binding Placeholder.Header}"
                          Content="{Binding Placeholder.Content}"
                          ActionButtonText="{Binding Placeholder.ActionButton}"
                          ActionButtonCommand="{Binding Placeholder.ActionButtonCommand}"/>
                </Grid>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
