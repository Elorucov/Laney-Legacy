<elor:Modal
    x:Class="Elorucov.Laney.Views.Modals.UserBlacklist"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Views.Modals"
    xmlns:elor="using:Elorucov.Toolkit.UWP.Controls"
    xmlns:vkui="using:VK.VKUI.Controls"
    xmlns:vk="using:ELOR.VKAPILib.Objects"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Style="{StaticResource VKUIFullModalStyle}" Title="{x:Bind Converter={StaticResource loc}, ConverterParameter=user_blacklist}">
    <Grid x:Name="LayoutRoot">
        <ListView VerticalAlignment="Top" ItemsSource="{Binding BannedUsers}" ItemTemplate="{StaticResource DefaultEntityWithoutSubtitleTemplate}" ItemContainerStyle="{StaticResource CommonListViewItemStyle}">
            <ListView.Header>
                <Border Height="8"/>
            </ListView.Header>
            <ListView.Footer>
                <Border MinHeight="8">
                    <vkui:Spinner VerticalAlignment="Center" Width="24" Height="24" Margin="12" Visibility="{Binding IsLoading}"/>
                </Border>
            </ListView.Footer>
        </ListView>
        <vkui:Placeholder Grid.ColumnSpan="2" VerticalAlignment="Stretch" 
                          Visibility="{Binding Placeholder, Converter={StaticResource ntv}}"
                          IconTemplate="{Binding Placeholder, Converter={StaticResource pic}}" 
                          Header="{Binding Placeholder.Header}"
                          Content="{Binding Placeholder.Content}"
                          ActionButtonText="{Binding Placeholder.ActionButton}"
                          ActionButtonCommand="{Binding Placeholder.ActionButtonCommand}"/>
    </Grid>
</elor:Modal>