<elor:Modal
    x:Class="Elorucov.Laney.Views.Modals.PlacePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Views.Modals"
    xmlns:elor="using:Elorucov.Toolkit.UWP.Controls"
    xmlns:vkui="using:VK.VKUI.Controls"
    xmlns:vk="using:ELOR.VKAPILib.Objects"
    xmlns:map="using:Windows.UI.Xaml.Controls.Maps"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Style="{StaticResource VKUIFullModalStyle}" Title="{x:Bind Converter={StaticResource loc}, ConverterParameter=a_geo}">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <map:MapControl x:Name="Map" Margin="8" ZoomLevel="16" Height="240" Center="{Binding CurrentPosition, Mode=TwoWay}" MapServiceToken="UiW7WOb2oHEe9yQSEVpg~53Z_RFeFeK1NCE1sIE_-Kw~Apg8QKsKCmrLWTa6BttNQVkUvaDQJjwCmgv8814SFKD2SmHWNIOJEUKpp8MDO-GP">
            <TextBlock Foreground="#000" FontFamily="Consolas" FontSize="10" TextWrapping="Wrap" Text="{Binding CurrentPosition, Converter={StaticResource geo}}"/>
        </map:MapControl>
        <Ellipse Fill="Red" Stroke="Black" StrokeThickness="2" Width="16" Height="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>


        <Button Margin="8" Grid.Row="2" Style="{StaticResource VKButtonPrimaryExtraLarge}" HorizontalAlignment="Stretch" Command="{Binding AttachCommand}" Content="{x:Bind Converter={StaticResource loc}, ConverterParameter=attach}"/>
    </Grid>
</elor:Modal>