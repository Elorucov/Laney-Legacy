<elor:Modal
    x:Class="Elorucov.Laney.Views.Modals.AuthWebView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Views.Modals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:elor="using:Elorucov.Toolkit.UWP.Controls"
    xmlns:vkui="using:VK.VKUI.Controls"
    mc:Ignorable="d"
    Style="{ThemeResource VKUITabModalStyle}" CloseButtonVisibility="Collapsed">
    <Grid>
        <WebView x:Name="Web"/>
        <Border x:Name="Loading" Background="{ThemeResource VKBackgroundContentBrush}">
            <vkui:Spinner Width="44" Height="44" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource VKAccentBrush}"/>
        </Border>
    </Grid>
</elor:Modal>
