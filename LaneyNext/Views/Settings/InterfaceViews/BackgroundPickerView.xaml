<st:SettingsPageBase
    x:Class="Elorucov.Laney.Views.Settings.InterfaceViews.BackgroundPickerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Views.Settings.InterfaceViews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:st="using:Elorucov.Laney.Views.Settings"
    xmlns:data="using:Elorucov.Laney.DataModels"
    xmlns:vkui="using:VK.VKUI.Controls"
    xmlns:prim="using:Elorucov.Laney.Controls.Primitives"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <prim:AppearancePreview Height="320"/>
        <GridView x:Name="Previews" Grid.Row="1" ItemsSource="{Binding Items}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="ItemPicked">
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="Margin" Value="2"/>
                    <Setter Property="Background" Value="Transparent"/>
                </Style>
            </GridView.ItemContainerStyle>
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:BackgroundPickerItem">
                    <Grid Background="{x:Bind PreviewBrush}" Width="80" Height="80" CornerRadius="4" RequestedTheme="{x:Bind Theme}">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8" Width="24" Height="24" ContentTemplate="{x:Bind Icon}" Foreground="{ThemeResource VKHeaderTintBrush}"/>
                        <TextBlock VerticalAlignment="Bottom" Margin="8" FontSize="14" Foreground="{ThemeResource VKHeaderTintBrush}" Text="{x:Bind Label}"/>
                        <Border BorderBrush="{ThemeResource SystemControlBackgroundBaseLowBrush}" BorderThickness="1" CornerRadius="4"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</st:SettingsPageBase>