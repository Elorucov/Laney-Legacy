<set:SettingsPageBase
    x:Class="Elorucov.Laney.Views.Settings.DebugViews.APIConsoleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Elorucov.Laney.Views.Settings.DebugViews"
    xmlns:vkui="using:VK.VKUI.Controls"
    xmlns:set="using:Elorucov.Laney.Views.Settings"
    xmlns:data="using:Elorucov.Laney.DataModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <set:SettingsPageBase.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="KeyValueTemplate" x:DataType="data:SimpleKeyValue">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox Style="{StaticResource VKCheckBox}" VerticalAlignment="Center" IsChecked="{x:Bind IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="6"/>
                    <TextBox Style="{StaticResource VKTextBox}" Grid.Column="1" Margin="6" Text="{x:Bind Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Key"/>
                    <TextBox Style="{StaticResource VKTextBox}" Grid.Column="2" Margin="6" Text="{x:Bind Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Value"/>
                    <Button Style="{StaticResource TransparentButtonStyle}" Grid.Column="3" Margin="6" VerticalAlignment="Center" Padding="8" Width="44" Height="44" ContentTemplate="{StaticResource Icon28DeleteOutline}" Foreground="{ThemeResource VKHeaderTintBrush}" Click="DeleteButton_Click"/>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </set:SettingsPageBase.Resources>

    <Grid Grid.Row="1" Margin="8" SizeChanged="ChangeLayout">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ListView x:Name="ParametersList" Grid.ColumnSpan="2" ItemsSource="{x:Bind MethodParameters}" ItemTemplate="{StaticResource KeyValueTemplate}" VerticalAlignment="Top" SelectionMode="None">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.Header>
                <TextBox x:Name="Method" Style="{StaticResource VKTextBox}" Margin="8" PlaceholderText="Method"/>
            </ListView.Header>
            <ListView.Footer>
                <Button Style="{StaticResource VKButtonPrimaryMedium}" Margin="8" Click="Button_Click" Content="Add"/>
            </ListView.Footer>
        </ListView>
        <TextBox x:Name="ResultBox" Grid.Row="1" Grid.ColumnSpan="2" Margin="8" Style="{StaticResource VKTextBox}" IsReadOnly="True" TextWrapping="Wrap" FontFamily="Consolas" PlaceholderText="Result"/>
    </Grid>
</set:SettingsPageBase>
